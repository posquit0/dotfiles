# INFO: `ghostty +list-keybinds --default` to get the default key bindings

# Send Alt/Meta when pressing Option (both keys), so terminal apps like Neovim can use <M-...> mappings.g. CodeCompanion's <M-A> chord).
macos-option-as-alt = true
